import {Metadata} from 'next';

export const metadata: Metadata = {
    title: "Nhiệm vụ được giao",
};

export default function NhiemVuDuocGiaoLayout({children}: { children: React.ReactNode }) {
    return children;
}