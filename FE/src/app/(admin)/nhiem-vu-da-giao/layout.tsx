import {Metadata} from 'next';

export const metadata: Metadata = {
    title: "Nhiệm vụ đã giao",
};

export default function NhiemVuDaGiaoLayout({children}: { children: React.ReactNode }) {
    return children;
}